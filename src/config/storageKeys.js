// src/config/storageKeys.js — Clés localStorage centralisées.
//
// Toutes les clés de stockage du jeu sont définies ici.
// Jamais hardcodées dans la logique — toujours importées depuis ce module.

/** Clé principale : profil complet (run incluse). */
export const PROFILE_STORAGE_KEY = 'duoforce_profile';
